# One-Time Docs Folder

Use this folder for temporary notes or one-off implementation memos.

Rules:

1. Only short-lived docs go here.
2. If a note becomes reusable, move it to `docs/` root or `docs/plans/`.
3. Remove stale one-time docs after merge.
